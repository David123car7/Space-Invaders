# Space-Invaders

Re-creation of Space Invaders game, built using  using C++ and the raylib library.

# How to run

## Build Raylib

```bash
mkdir build && cd build
cmake ..
make
sudo make install
sudo ldconfig
```

## Build Game

```bash
cmake -S . -B build
cmake --build build
```









